{
  "name": "xgraph",
  "version": "1.3.7",
  "description": "An Executable Graph Database",
  "main": "./src/xgraph.js",
  "scripts": {
    "build": "cd build && npm run build",
    "test": "npm install --production --verbose && cd Tests && npm install --verbose && node test.js && cd .. && npm run lint && echo Test Suite Finished with exit code 0!",
    "qa": "npm install --production --verbose && cd Tests && npm install --verbose && node test.js --fast && cd .. && npm run lint && echo Test Suite Finished with exit code 0!",
    "lint": "eslint src/*.js --fix"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/IntrospectiveSystems/xGraph.git"
  },
  "author": "Introspective Systems, LLC.",
  "bugs": {
    "url": "https://github.com/IntrospectiveSystems/xGraph/issues"
  },
  "bin": {
    "xgraph": "./src/xgraph.js"
  },
  "homepage": "https://github.com/IntrospectiveSystems/xGraph#readme",
  "license": "AGPL-3.0",
  "dependencies": {
    "jszip": "3.1.5",
    "strip-comments": "1.0.2",
    "uuid": "3.2.1"
  },
  "devDependencies": {
    "eslint": "5.1.0"
  }
}
